namespace WebAppCoreDb.Models.Helpers
{
    public class Helpers : HelpersBase
    {
        
    //    public override IActionResult RedirectToLocal(string returnUrl)
       
    //        {
    //            //return Url.IsLocalUrl(returnUrl)
    //            //      ? Redirect(returnUrl)
    //            //      : (IActionResult)RedirectToAction(nameof(HomeController.Index), "Home");
    //        }
    //}
     
    //   private void AddErrors(IdentityResult result)
    //{
    //    foreach (var error in result.Errors)
    //        ModelState.Add(string.Empty, error.Description);
        
    //}
    
}
}
