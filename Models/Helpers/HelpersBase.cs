using Microsoft.AspNetCore.Mvc;

namespace WebAppCoreDb.Models.Helpers
{
    public abstract class HelpersBase
    {
        //public abstract IActionResult RedirectToLocal(string returnUrl)

        
        //{
        //    return Url.IsLocalUrl(returnUrl)
        //          ? Redirect(returnUrl)
        //          : (IActionResult)RedirectToAction(nameof(HomeController.Index), "Home");
        //}
    }
}